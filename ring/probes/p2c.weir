let ev = Env.ofPairs [("RING_PROBE", "42")]

within proc child = sh -c "echo env is $RING_PROBE; sleep 0.3; exit 7" !ev
    poll timeout=5s interval=50ms
        not (Proc.running child)
    let code = Proc.wait child
    print $"exit={code} tail={Proc.tail child |> Str.join "/"}"
